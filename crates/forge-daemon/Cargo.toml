[package]
name = "forge-daemon"
description = "The Rubick Forge daemon: HTTP/WS API, tmux, git, SQLite, adapters"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[[bin]]
name = "forge-daemon"
path = "src/main.rs"

[dependencies]
forge-core = { workspace = true }
