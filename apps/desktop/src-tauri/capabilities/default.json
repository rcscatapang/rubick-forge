{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main window: core APIs only. The daemon is reached over HTTP/WS from the webview; no filesystem, process or shell permissions are granted here (D3).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
