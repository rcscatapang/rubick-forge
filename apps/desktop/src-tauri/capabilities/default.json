{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. The daemon is reached over HTTP/WS from the webview; the only native capabilities are a folder picker for registering a repository and opening a path in Finder or an editor.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-path",
    "opener:allow-open-url"
  ]
}
